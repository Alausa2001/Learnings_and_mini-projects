#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>
#include <stdbool.h>
#include <string.h>

int randomization(void);
char* add_dashes(char *str);
char* fill(char *str, char a, char b);

#endif
